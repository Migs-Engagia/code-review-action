name: "Claude Code Review"
description: "Use Claude (Anthropic) to review code changes in a PR"
author: "Your Name"
inputs:
  anthropic-api-key:
    description: "Your Anthropic API key"
    required: true
outputs:
  claude-review:
    description: "Response from Claude"
runs:
  using: "node20"
  main: "index.js"
branding:
  icon: "eye"
  color: "gray-dark"
